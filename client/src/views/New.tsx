import React from "react";

import layout from "../styles/_layout.module.scss";

const New = () => {
  return (
    <main className={layout.page}>
      <div>New</div>
    </main>
  );
};

export default New;
